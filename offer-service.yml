server:
  port: 8088

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/eatease_offers?createDatabaseIfNotExist=true
    username: root
    password: Vissu408@@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
