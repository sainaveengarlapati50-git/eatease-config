server:
  port: 8084

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/eatease_payments?createDatabaseIfNotExist=true
    username: root
    password: Naveen#450
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

stripe:
  secret-key: sk_test_XXXXXXXXXXXXXXXXXXXX

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
