server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/eatease_users?createDatabaseIfNotExist=true
    username: root
    password: Naveen#450
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: charithkumar28@gmail.com
    password: xihpzufyfhbaartp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

eureka:
  client:
    enabled: true
    service-url:
      defaultZone: http://localhost:8761/eureka/

  instance:
    prefer-ip-address: true

app:
  jwt:
    secret: ${JWT_SECRET:EatEaseFoodDeliveryPlatformSecretKeyForJWTTokenGeneration2024@Secure@JWT@256}
    expiration-ms: 86400000

  dev:
    allow-public-approve: ${ALLOW_PUBLIC_APPROVE:false}
